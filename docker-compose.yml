version: '3.8'

services:
  openalpha:
    build: .
    container_name: openalpha-brain
    env_file:
      - .env
    volumes:
      - ./sessions:/app/sessions
      - ./runtime:/app/runtime
    restart: unless-stopped